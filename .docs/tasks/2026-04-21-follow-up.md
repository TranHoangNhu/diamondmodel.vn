# 2026-04-21 Follow-up

## Current state

- H1 and H2 use `SVN-Moneta` at `44px`.
- Main navigation uses `16px`.
- H3 and body text use `Mulish`.
- The reusable `background-image` skill has been generalized.

## Tomorrow's tasks

- [ ] Re-open the main routes and verify heading sizes still read correctly on desktop and mobile.
- [ ] Check for any remaining white wrappers or reveal layers that can hide the background texture.
- [ ] Run a screenshot pass on homepage, archive pages, and contact page.
- [ ] Clean any leftover temporary files, then commit and push the final state if the visual pass is clean.
