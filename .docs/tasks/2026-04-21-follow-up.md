# 2026-04-21 Follow-up

## Current state

- H1 and H2 use `SVN-Moneta` at `44px`.
- Main navigation uses `16px`.
- H3 and body text use `Mulish`.
- The reusable `background-image` skill has been generalized.
- Visual QA completed on desktop and mobile for homepage, archive pages, and contact page.

## Tomorrow's tasks

- [x] Re-open the main routes and verify heading sizes still read correctly on desktop and mobile.
- [x] Check for any remaining white wrappers or reveal layers that can hide the background texture.
- [x] Run a screenshot pass on homepage, archive pages, and contact page.
- [x] Clean any leftover temporary files, then commit and push the final state if the visual pass is clean.

## Result

- The page background now shows through consistently across the main routes.
- No new code changes were needed after the QA pass.
